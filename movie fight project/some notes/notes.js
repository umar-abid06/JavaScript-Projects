// DEBOUNCING AN INPUT:
// waiting for some time to pass after the last event to actually do something

// ...args: take all the different arguments into an array
// function.apply(null, args): take all the arguments from the args array and pass them in as separate arguments to the original function
